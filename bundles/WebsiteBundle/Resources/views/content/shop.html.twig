{% extends '@Website/base.html.twig' %}

{% block content %}
  <div class="tw-container tw-mt-10">
    <h1 class="tw-font-bold tw-mb-3 tw-text-xl">{{pimcore_input('home-h1', {placeholder: "Headline..."})}}</h1>
    <p class="tw-mb-3">{{pimcore_input('home-p', {placeholder: 'Lorem ipsum...'})}}</p>

    <div class="tw-flex tw-gap-x-7 tw-mt-5">
      {% for car in cars %}
        {% include "@Website/components/CarTeaser/index.html.twig" with {'car': car} %}
      {% endfor %}
    </div>

  </div>

{% endblock %}
